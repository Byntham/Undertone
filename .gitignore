# Virtualenv and caches
.venv/
.venv-qt/
spikes/out/
spikes/cleanup_prompt_100/server_url.txt
spikes/cleanup_prompt_100/server_stdout.log
spikes/cleanup_prompt_100/server_stderr.log
__pycache__/
*.pyc

# PyInstaller output (the .spec IS committed)
build/
dist/

# Electron / TypeScript output
electron/node_modules/
electron/dist/
electron/coverage/

# Transient agent worktrees (a second checkout appears here during agent
# runs — without this, git add -A would sweep in thousands of files)
.claude/worktrees/


