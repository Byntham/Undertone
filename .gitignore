# Virtualenv and caches
.venv/
.venv-qt/
__pycache__/
*.pyc

# PyInstaller output (the .spec IS committed)
build/
dist/

# Transient agent worktrees (a second checkout appears here during agent
# runs — without this, git add -A would sweep in thousands of files)
.claude/worktrees/



